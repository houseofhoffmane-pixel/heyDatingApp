{
  "name": "hey",
  "version": "0.1.0",
  "private": true,
  "description": "Hey — dating app. Single Node process serves API + WS + SPA. Builds web/, copies to backend/public/, runs Nest.",
  "scripts": {
    "install:all":  "npm --prefix backend install && npm --prefix web install",
    "postinstall":  "npm run install:all",
    "build:web":    "npm --prefix web run build && node scripts/copy-web-build.mjs",
    "build:backend":"npm --prefix backend run build",
    "build":        "npm run install:all && npm run build:web && npm run build:backend",
    "prisma:migrate": "npm --prefix backend run prisma:migrate",
    "db:seed":        "npm --prefix backend run db:seed",
    "start":          "node backend/dist/main.js",
    "dev:web":        "npm --prefix web run dev",
    "dev:backend":    "npm --prefix backend run start:dev",
    "smoke":          "bash scripts/smoke.sh"
  },
  "engines": {
    "node": ">=18"
  }
}
